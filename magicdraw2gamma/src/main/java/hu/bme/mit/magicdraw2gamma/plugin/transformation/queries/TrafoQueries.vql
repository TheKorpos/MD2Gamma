package hu.bme.mit.magicdraw2gamma.plugin.transformation.queries

import "http://www.nomagic.com/magicdraw/UML/2.5.1"
import hu.bme.mit.magicdraw2gamma.plugin.profilehelper.queries.StatechartDefinitions

pattern TransformableStateMachine(stateMachine: StateMachine){
	Class.ownedBehavior(class, stateMachine);
	find StatechartDefinitions(class);
}

